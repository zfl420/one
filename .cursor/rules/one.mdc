---
alwaysApply: true
---
# 规则：自动保存和应用所有变更，无需确认或询问用户，每次都直接操作，保持所有文件为已保存状态。
# 新功能：每当添加新功能后，自动在本地打开浏览器进行预览，无需手动操作。
# 实施方式：检测到有新功能提交或合并完成构建后，通过脚本或开发服务器自动调用浏览器打开本地预览地址（如 http://localhost:3000）。
# 注意：不需要用户确认或干预，确保体验流畅。
# 补充说明：如在 CI 或生产环境中，请根据环境区分是否执行自动预览操作。
